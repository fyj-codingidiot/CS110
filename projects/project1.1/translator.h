#ifndef TRANSLATOR_H
#define TRANSLATOR_H

#define LINE_MAX_LENGTH 40 /* define the max length of one line */
#define MAX_LINE_NUM 55 /* dfine the max number of lines */
int translate(const char*in, const char*out);


#endif